function calcular_area_circulo()
{
    const radio = 
    const resultado = parseInt(radio) Math.pow(radio,2) 
    return resultado
}
function mostrar()
{
    const respuesta = calcular_area_circulo()
    alert(respuesta)
}