function calcular()
{
    const a = prompt('ingresa un cateto:');
    const b = prompt('ingresar otro cateto:');
    const resultado = Math.sqrt(Math.pow(a2+b2))
    return resultado
}

function mostrar()
{
    const respuesta = calcular()
    alert(respuesta)
}